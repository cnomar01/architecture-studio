export default function Loading() {
  return (
    <main className="flex min-h-[60vh] items-center justify-center bg-black text-white">
      <span className="text-[10px] uppercase tracking-[0.35em] text-white/50">Loading</span>
    </main>
  );
}
