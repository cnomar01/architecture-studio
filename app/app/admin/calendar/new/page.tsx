"use client";
import {useState} from "react";import {useRouter} from "next/navigation";import {addCalendarEvent,type CalendarEventType} from "../calendarStore";
export default function NewCalendarEvent(){const r=useRouter();const [title,setTitle]=useState("");const [date,setDate]=useState(new Date().toISOString().slice(0,10));const [type,setType]=useState<CalendarEventType>("Deadline");const [project,setProject]=useState("City Edge Mall");function submit(e:React.FormEvent){e.preventDefault();if(!title)return;addCalendarEvent({title,date,type,project,projectId:"CEM-001",status:"Planned"});r.push("/app/admin/calendar");}return <main className="min-h-screen bg-[#111] px-6 py-10 text-white"><div className="mx-auto max-w-xl"><h1 className="text-3xl font-semibold">Add Calendar Event</h1><p className="mt-2 text-sm text-white/35">Create a schedule item for the studio.</p><form onSubmit={submit} className="mt-8 space-y-5 rounded-3xl border border-white/10 bg-white/[.03] p-6"><label className="block text-xs text-white/45">Title<input value={title} onChange={e=>setTitle(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm outline-none"/></label><label className="block text-xs text-white/45">Date<input type="date" value={date} onChange={e=>setDate(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm outline-none"/></label><label className="block text-xs text-white/45">Type<select value={type} onChange={e=>setType(e.target.value as CalendarEventType)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm outline-none">{["Task","Site Visit","Deadline","Meeting","Milestone"].map(x=><option key={x}>{x}</option>)}</select></label><label className="block text-xs text-white/45">Project<input value={project} onChange={e=>setProject(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 p-3 text-sm outline-none"/></label><button className="w-full rounded-xl bg-white p-3 text-xs font-medium text-black">Create Event</button></form></div></main>}
