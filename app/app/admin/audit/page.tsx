"use client";

import { useEffect, useMemo, useState } from "react";
import { Activity, Filter, ShieldCheck, UserRound } from "lucide-react";
import { getStudioOverview, type OverviewActivity } from "@/lib/client/studioOverview";

export default function AuditPage(){const [entries,setEntries]=useState<OverviewActivity[]>([]);const [query,setQuery]=useState("");const [area,setArea]=useState("All");const [error,setError]=useState("");useEffect(()=>{void getStudioOverview().then((data)=>setEntries(data.activity)).catch((cause)=>setError(cause instanceof Error?cause.message:"Could not load audit activity."))},[]);const areas=useMemo(()=>["All",...Array.from(new Set(entries.map((item)=>item.entity_type))).sort()],[entries]);const filtered=useMemo(()=>{const q=query.trim().toLowerCase();return entries.filter((item)=>(area==="All"||item.entity_type===area)&&(!q||`${item.action} ${item.actor_name||""} ${item.entity_type} ${item.entity_id||""}`.toLowerCase().includes(q)))},[entries,query,area]);return <main className="mx-auto max-w-6xl p-8"><div className="mb-2 flex items-center gap-3"><Activity size={24}/><h1 className="text-3xl font-semibold">Audit & Activity</h1></div><p className="opacity-65">Real actions recorded by the shared server audit trail.</p>{error&&<p role="alert" className="mt-5 rounded-xl border border-red-400/30 p-4 text-sm text-red-300">{error}</p>}<div className="mt-7 flex flex-wrap gap-3"><div className="relative min-w-[280px] flex-1"><Activity size={16} className="absolute left-4 top-3.5 opacity-50"/><input value={query} onChange={(event)=>setQuery(event.target.value)} placeholder="Search audit activity…" className="h-11 w-full rounded-xl border bg-transparent pl-10 pr-4"/></div><div className="flex items-center gap-2"><Filter size={16}/><select value={area} onChange={(event)=>setArea(event.target.value)} className="h-11 rounded-xl border bg-transparent px-3">{areas.map((item)=><option key={item} className="bg-[#111]">{item}</option>)}</select></div></div><div className="mt-6 grid gap-3">{filtered.map((item)=><div key={item.id} className="flex items-center gap-4 rounded-xl border p-4"><ShieldCheck size={19} className="opacity-60"/><div className="flex-1"><div className="font-medium">{humanize(item.action)}</div><div className="mt-1 text-sm opacity-55">{item.entity_type}{item.entity_id?` · ${item.entity_id}`:""} · {new Date(item.created_at).toLocaleString()}</div></div><div className="flex items-center gap-2 text-sm opacity-65"><UserRound size={15}/>{item.actor_name||"System"}</div></div>)}{!filtered.length&&!error&&<div className="p-10 text-center opacity-55">No audit activity found.</div>}</div></main>}
const humanize=(value:string)=>value.replace(/[._-]+/g," ").replace(/\b\w/g,(letter)=>letter.toUpperCase());
