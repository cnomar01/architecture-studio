"use client";
import {useEffect,useState} from "react"; import {getClients,Client} from "@/lib/core/clientStore";
export default function ClientsPage(){const [items,setItems]=useState<Client[]>([]);useEffect(()=>setItems(getClients()),[]);return <main className="mx-auto max-w-6xl p-6"><h1 className="text-2xl font-semibold">Clients</h1><div className="mt-6 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{items.map(c=><article key={c.id} className="rounded-2xl border p-5"><div className="font-semibold">{c.name}</div><div className="mt-1 text-sm opacity-70">{c.company||"Individual Client"}</div><div className="mt-4 text-sm">{c.email||"—"}</div><div className="text-sm">{c.phone||"—"}</div><div className="mt-3 text-xs opacity-60">{c.status}</div></article>)}</div>{!items.length&&<div className="mt-8 rounded-2xl border p-8 text-center opacity-60">No clients yet.</div>}</main>}
