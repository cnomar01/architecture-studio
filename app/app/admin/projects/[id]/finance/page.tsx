"use client";
import Link from "next/link"; import {useEffect,useState} from "react"; import {ArrowLeft,Plus,TrendingUp,Wallet} from "lucide-react";
import {getProjectById} from "@/lib/core/projectStore"; import {getProjectFinance,getProjectIncome,getProjectExpenses,getProjectProfit,getProjectProfitMargin,getBudgetVsActual,getProjectBudget,getProjectInvoices,formatAmount} from "@/app/app/admin/finance/financeStore";
export default function ProjectFinancePage(){const [tick,setTick]=useState(0); const id=typeof window!=="undefined"?window.location.pathname.split("/").filter(Boolean).at(-2):undefined; const project=id?getProjectById(id):null; useEffect(()=>{const t=window.setInterval(()=>setTick(x=>x+1),1500);return()=>window.clearInterval(t)},[]); if(!project)return <main className="min-h-screen bg-[#080808] p-10 text-white">Project not found.</main>; const income=getProjectIncome(project.id),expenses=getProjectExpenses(project.id),profit=getProjectProfit(project.id),margin=getProjectProfitMargin(project.id),budget=getBudgetVsActual(project.id),b=getProjectBudget(project.id),transactions=getProjectFinance(project.id),invoices=getProjectInvoices(project.id); return <main className="min-h-screen bg-[#080808] text-white"><div className="mx-auto max-w-[1400px] px-6 py-8 lg:px-10"><Link href={`/app/admin/projects/${project.id}`} className="inline-flex items-center gap-2 text-sm text-white/40"><ArrowLeft size={15}/>Back to Project</Link><div className="mt-8 flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-xs uppercase tracking-[.28em] text-white/30">Project Finance</p><h1 className="mt-2 text-4xl font-semibold">{project.name}</h1><p className="mt-2 text-sm text-white/35">Budget, actual cost, profitability and invoices.</p></div><Link href={`/app/admin/finance/new?project=${project.id}`} className="inline-flex items-center gap-2 rounded-full bg-white px-5 py-3 text-sm font-medium text-black"><Plus size={16}/>Add Transaction</Link></div><div className="mt-8 grid gap-4 md:grid-cols-2 xl:grid-cols-5">{[["Revenue",income],["Actual Cost",expenses],["Net Profit",profit],["Margin",margin],["Budget Used",budget.utilization]].map(([l,v])=><div key={String(l)} className="rounded-3xl border border-white/10 bg-white/[.025] p-6"><p className="text-xs uppercase tracking-[.16em] text-white/30">{l}</p><p className="mt-5 text-2xl font-semibold">{l==="Margin"||l==="Budget Used"?`${v}%`:formatAmount(Number(v),"EGP")}</p></div>)}</div><div className="mt-6 grid gap-4 lg:grid-cols-2"><div className="rounded-3xl border border-white/10 bg-white/[.025] p-6"><p className="text-xs uppercase tracking-[.18em] text-white/30">Budget vs Actual</p><div className="mt-6 grid grid-cols-2 gap-4"><div><p className="text-xs text-white/30">Budget</p><p className="mt-2 text-xl">{formatAmount(budget.budget,"EGP")}</p></div><div><p className="text-xs text-white/30">Actual Cost</p><p className="mt-2 text-xl">{formatAmount(budget.actualCost,"EGP")}</p></div></div><div className="mt-6 h-2 rounded-full bg-white/10"><div className="h-full bg-white" style={{width:`${Math.min(100,budget.utilization)}%`}}/></div><p className="mt-2 text-xs text-white/25">Variance: {formatAmount(budget.variance,"EGP")}</p></div><div className="rounded-3xl border border-white/10 bg-white/[.025] p-6"><p className="text-xs uppercase tracking-[.18em] text-white/30">Contract & Billing</p><div className="mt-5 space-y-4"><Row label="Contract Value" value={formatAmount(budget.contractValue,"EGP")}/><Row label="Budget" value={formatAmount(budget.budget,"EGP")}/><Row label="Invoices" value={String(invoices.length)}/><Row label="Received Income" value={formatAmount(income,"EGP")}/></div>{!b&&<p className="mt-5 text-xs text-white/25">No project budget has been configured yet.</p>}</div></div><section className="mt-6 rounded-3xl border border-white/10 bg-white/[.025] p-6"><div className="flex items-center justify-between"><div><p className="text-xs uppercase tracking-[.18em] text-white/30">Financial Activity</p><h2 className="mt-2 text-xl">Transactions</h2></div><span className="text-xs text-white/30">{transactions.length} records</span></div><div className="mt-5 divide-y divide-white/6">{transactions.map(x=><div key={x.id} className="flex flex-col gap-2 py-4 md:flex-row md:justify-between"><div><p className="text-sm">{x.description}</p><p className="mt-1 text-xs text-white/30">{x.id} · {x.category} · {x.date} · {x.status}</p></div><p className={x.type==="Income"?"text-emerald-300/80":"text-red-300/75"}>{x.type==="Income"?"+":"-"}{formatAmount(x.amount,x.currency)}</p></div>)}{!transactions.length&&<p className="py-10 text-center text-sm text-white/30">No financial activity yet.</p>}</div></section></div></main>}
function Row({label,value}:{label:string;value:string}){return <div className="flex justify-between gap-4"><span className="text-sm text-white/35">{label}</span><span className="text-sm text-white/70">{value}</span></div>}
