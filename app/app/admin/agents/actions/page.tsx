"use client";
import {useEffect,useState} from "react";import {CheckCircle2,Clock3,ShieldCheck,Zap} from "lucide-react";import {getAgentActions,AgentAction} from "../actionStore";
export default function ActionsPage(){const[items,setItems]=useState<AgentAction[]>([]);useEffect(()=>setItems(getAgentActions()),[]);const applied=items.filter(x=>x.status==="Applied").length;return <div className="min-h-screen bg-white text-black"><div className="mx-auto max-w-6xl px-5 py-8 md:px-8"><header className="border-b border-black/10 pb-7"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[.24em] text-black/45"><Zap size={14}/> AI Action Center</div><h1 className="mt-2 text-3xl font-semibold tracking-tight md:text-5xl">Actions</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-black/60">A durable local log of AI-proposed actions and their execution state.</p></header><div className="mt-6 grid gap-3 md:grid-cols-3"><div className="rounded-2xl border border-black/10 p-5"><div className="text-[10px] uppercase tracking-[.18em] text-black/35">Total</div><div className="mt-2 text-3xl font-semibold">{items.length}</div></div><div className="rounded-2xl border border-black/10 p-5"><div className="text-[10px] uppercase tracking-[.18em] text-black/35">Applied</div><div className="mt-2 text-3xl font-semibold">{applied}</div></div><div className="rounded-2xl border border-black/10 p-5"><div className="text-[10px] uppercase tracking-[.18em] text-black/35">Pending</div><div className="mt-2 text-3xl font-semibold">{items.length-applied}</div></div></div><div className="mt-6 space-y-3">{items.length===0?<div className="rounded-2xl border border-dashed border-black/15 p-12 text-center text-sm text-black/45">No AI actions recorded yet. Run an agent and choose “Plan actions”.</div>:items.map(a=><div key={a.id} className="rounded-2xl border border-black/10 p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2"><span className="rounded-full bg-black px-2 py-1 text-[9px] font-semibold uppercase text-white">{a.type}</span><span className="text-sm font-semibold">{a.title}</span></div><p className="mt-2 text-xs leading-5 text-black/55">{a.description}</p><div className="mt-3 text-[10px] text-black/40">{a.project} · {a.priority} · Agent: {a.sourceAgent}</div></div><div className="text-[10px]">{a.status==="Applied"?<span className="flex items-center gap-1 text-emerald-700"><CheckCircle2 size={13}/> Applied {a.linkedId?`· ${a.linkedId}`:""}</span>:a.status==="Dismissed"?<span className="text-black/35">Dismissed</span>:<span className="flex items-center gap-1 text-amber-700"><Clock3 size={13}/> Suggested</span>}</div></div></div>)}</div><div className="mt-5 flex items-center gap-2 text-[10px] text-black/35"><ShieldCheck size={12}/> Execution is explicit and owner-controlled.</div></div></div>}
