"use client";
import {useState} from "react";
import Link from "next/link";
import {Shell,Grid} from "../shared";
import {getProgress,updateProgress} from "../operationsStore";
export default function Page(){const [items,setItems]=useState(getProgress()); function bump(id:string,actual:number){setItems(updateProgress(id,{actual,status:actual>=items.find(x=>x.id===id)!.planned?"On Track":"At Risk"}))} return <Shell title="Progress Control" desc="{desc}"><div className="space-y-4">{items.map(x=>{const variance=x.actual-x.planned;return <div key={x.id} className="rounded-2xl border border-white/10 p-5"><div className="flex justify-between"><div><div className="text-xs text-white/30">{x.projectId}</div><div className="mt-2 font-semibold">{x.phase}</div></div><div className="text-xs">{x.status}</div></div><div className="mt-5 h-2 overflow-hidden rounded-full bg-white/10"><div className="h-full bg-white" style={{width:`${Math.min(100,x.actual)}%`}}/></div><div className="mt-4 grid grid-cols-2 gap-3 text-xs text-white/45 md:grid-cols-4"><span>Planned {x.planned}%</span><span>Actual {x.actual}%</span><span>Baseline {x.baseline}%</span><span>Forecast {x.forecast}%</span></div><button onClick={()=>bump(x.id,Math.min(100,x.actual+5))} className="mt-4 rounded-lg border border-white/10 px-3 py-2 text-xs">Log +5% progress</button><span className="ml-3 text-xs text-white/30">Variance {variance}%</span></div>})}</div></Shell>}
