import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import { getPublishedWebsiteProject } from "@/lib/server/websiteProjects";

type Props={params:Promise<{slug:string}>};
export const dynamic="force-dynamic";
export async function generateMetadata({params}:Props):Promise<Metadata>{const {slug}=await params;const project=await getPublishedWebsiteProject(slug);return project?{title:project.title,description:project.description,alternates:{canonical:`/projects/${project.slug}`},openGraph:{title:project.title,description:project.description,images:[project.image_url]}}:{}}
export default async function ProjectPage({params}:Props){const {slug}=await params;const project=await getPublishedWebsiteProject(slug);if(!project)notFound();const gallery=project.gallery.length?project.gallery:[project.image_url];return <main className="bg-black text-white"><section className="relative min-h-screen overflow-hidden"><img src={project.image_url} alt={project.title} className="absolute inset-0 h-full w-full object-cover"/><div className="absolute inset-0 bg-gradient-to-t from-black via-black/25 to-black/10"/><div className="relative z-10 flex min-h-screen flex-col justify-end px-6 pb-10 md:px-10 md:pb-14 lg:px-16"><div className="flex flex-col gap-8 lg:flex-row lg:items-end lg:justify-between"><div><p className="mb-5 text-[9px] uppercase tracking-[.3em] text-white/60">{project.category}</p><h1 className="max-w-[1200px] font-[var(--font-display)] text-[64px] uppercase leading-[.78] tracking-[-.035em] sm:text-[82px] md:text-[120px] lg:text-[150px]">{project.title}</h1></div><div className="text-[10px] uppercase tracking-[.2em] text-white/65 lg:text-right"><p>{project.location}</p><p className="mt-2">{project.year}</p></div></div></div></section><section className="px-6 py-24 md:px-10 md:py-32 lg:px-16"><div className="grid gap-12 lg:grid-cols-12"><p className="text-[9px] uppercase tracking-[.3em] text-white/40 lg:col-span-3">Overview</p><p className="text-[25px] leading-[1.08] tracking-[-.025em] sm:text-[32px] md:text-[46px] lg:col-span-8 lg:col-start-5">{project.description}</p></div></section><section className="bg-[#f2f2f0] px-6 py-20 text-black md:px-10 md:py-28 lg:px-16"><p className="text-[9px] uppercase tracking-[.3em] text-black/45">Project Details</p><div className="mt-10 grid gap-8 sm:grid-cols-3"><div><p className="text-xs uppercase tracking-[.2em] text-black/45">Type</p><p className="mt-3 text-lg">{project.category}</p></div><div><p className="text-xs uppercase tracking-[.2em] text-black/45">Location</p><p className="mt-3 text-lg">{project.location}</p></div><div><p className="text-xs uppercase tracking-[.2em] text-black/45">Timeline</p><p className="mt-3 text-lg">{project.year}</p></div></div></section><section className="px-6 py-24 md:px-10 md:py-32 lg:px-16"><div className="mb-12"><p className="text-[9px] uppercase tracking-[.3em] text-white/40">Project Documentation</p><h2 className="mt-5 font-[var(--font-display)] text-[54px] uppercase leading-[.8] tracking-[-.03em] sm:text-[72px] md:text-[110px]">The Work.</h2></div><div className="grid gap-6 md:grid-cols-2">{gallery.map((image,index)=><div key={`${image}-${index}`} className={`overflow-hidden bg-white/5 ${index===0?"md:col-span-2 aspect-[16/9]":"aspect-[4/3]"}`}><img src={image} alt={`${project.title} — ${index+1}`} className="h-full w-full object-cover"/></div>)}</div></section><footer className="border-t border-white/15 px-6 py-10 md:px-10 lg:px-16"><Link href="/projects" className="text-[9px] uppercase tracking-[.25em] text-white/60">← All Projects</Link></footer></main>}
